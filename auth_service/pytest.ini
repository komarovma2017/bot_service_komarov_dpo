# pytest.ini для Auth Service
# pythonpath = . — чтобы пакет app был доступен при запуске тестов
# asyncio_mode = auto — чтобы async-фикстуры работали без декораторов

[pytest]
pythonpath = .
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
